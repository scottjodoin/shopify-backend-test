<!DOCTYPE html>
<html>
  <head>
    <title>Scimgur: Home of super-cool images.</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.1/dist/css/bootstrap.min.css">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.1/dist/js/bootstrap.min.js"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <script src="https://code.jquery.com/jquery-3.6.0.min.js" integrity="sha256-/xUj+3OJU5yExlq6GSYGSHk7tPXikynS7ogEvDej/m4=" crossorigin="anonymous"></script>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container-fluid">
        <%= link_to 'Scimgur', posts_path, class: "navbar-brand" %>
      </div>

      <div class="collapse navbar-collapse">
        <% if not user_signed_in? %>
          <%= link_to "Sign up", "/users/sign_up", class: "btn btn-outline-primary text-nowrap me-2" %>
        <% else %>
          <%= link_to user.username, "/users/edit", action: :edit, class: "btn btn-outline-primary text-nowrap me-2" %>
        <% end %>

        <%= link_to new_post_path, class: "float-end text-nowrap btn btn-success" do %>
          <i class="fa fa-upload me-1"></i>Upload images
        <% end %>
      </div>
    </nav>
    <% if notice.present? or alert.present? %>
    <div class="container">
      <div class="alert alert-success"><%= notice %></div>
      <div id="alert" class="alert alert-warning"><%= alert %></div>
    </div>
    <% end %>
    <div class="container">
      <%= yield %>
    </div>
  </body>
</html>
